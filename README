==== Zolden DBMS ====
This program is an example of a database management system.
Note: it is Linux-only!

== Compilation ==
git clone https://github.com/numberZero/database
cd database
mkdir build
cd build
cmake ..
make

== Options ==
- USE_EXCLUSIVE_LOCK [server] - disallow requests to be processed simultaneously
    instead of default SRXW (shared read or exclusive write) policy
- USE_MANUAL_TCP_FLUSH [server, client] - instead of system default settings
    note: currently client is unaware of this option
- DISCONNECT_ON_INVALID_REQUEST [server]- ...
